#!/usr/bin/php
<?php

$path = dirname(dirname(__DIR__)) . DIRECTORY_SEPARATOR . 'applications' . DIRECTORY_SEPARATOR;

echo <<<EOF
WebSocketServer {
	enable;
}

Workbreeze {
	path '{$path}workbreeze.php';
}

WorkbreezeNotifier {
	path '{$path}notifier.php';
}

EOF;

